{
  "name": "@dashsgs/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos e contratos compartilhados entre a API e o front (doc 04 §5)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(sem testes próprios — contratos são exercitados pela api/web)\" && exit 0",
    "test:cov": "echo \"(sem testes próprios)\" && exit 0",
    "clean": "rimraf dist"
  }
}
