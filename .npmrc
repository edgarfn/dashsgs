# Instalação determinística e segura
engine-strict=true
auto-install-peers=true
strict-peer-dependencies=false
# Builds de scripts pós-instalação só para pacotes explicitamente aprovados (supply chain)
# Scripts de pos-instalacao apenas para a allowlist em package.json#pnpm.onlyBuiltDependencies
